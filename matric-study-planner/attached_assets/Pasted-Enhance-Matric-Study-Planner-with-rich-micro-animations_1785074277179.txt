Enhance "Matric Study Planner" with rich micro-animations throughout the app. Install framer-motion if not already present (npm install framer-motion). Apply the following across all existing screens (onboarding, dashboard, syllabus tracker, bottom nav):

1. PAGE TRANSITIONS
- Wrap route changes in <AnimatePresence> with a subtle slide + fade (x: 20 → 0, opacity: 0 → 1) so each screen slides in from the right like a native app

2. ONBOARDING
- Each step transitions with a slide-fade (not a hard cut)
- Progress dots at top animate/fill as user advances
- "Next" button has a subtle scale-down on tap (whileTap={{ scale: 0.95 }})

3. BOTTOM NAV
- Active tab icon gets a spring-based scale bounce when selected
- Small animated indicator (dot or pill background) slides between tabs using layoutId for shared-element transition

4. SYLLABUS TRACKER
- Accordion expand/collapse should use height animation (framer-motion's AnimatePresence + motion.div with initial/animate/exit height), not abrupt show/hide
- Checkbox tap: checkmark draws in with a small scale+rotate pop, plus a satisfying "settle" bounce
- When a chapter is checked, briefly highlight the row (soft green flash fading out over 500ms)
- Progress bar fill animates smoothly (transition: width 0.6s ease-out) whenever percentage changes — don't just snap to new value
- When a subject reaches 100%, trigger a small celebratory animation on that card (e.g. brief scale pulse + a checkmark badge popping in)

5. DASHBOARD
- Progress ring (circular) animates its stroke-dashoffset from 0 to target value on mount/update, not instant jump
- Countdown number ("Days left: X") uses a subtle number-flip or fade-in-up animation on load
- Task cards stagger-animate in on page load (each card fades/slides up with a small delay offset, e.g. 50-80ms between cards) using staggerChildren in a parent motion.div

6. GENERAL POLISH
- Buttons: whileHover scale 1.02, whileTap scale 0.96 on all primary buttons
- Cards: subtle lift on tap (translateY -2px + shadow increase) for tactile feedback
- Loading states: skeleton shimmer instead of blank space if any async load occurs
- Keep animation durations short (150-400ms) — snappy, not sluggish, since this is a student productivity app not a showcase site

IMPORTANT: Do not sacrifice performance for animation. Keep all transitions GPU-friendly (transform/opacity based, avoid animating layout-triggering properties like width/height where possible — use scaleX for progress bars instead if performance matters, or keep width animation since it's just one bar).

Do not change any functionality, data logic, or add new features — this is a pure visual/animation polish pass on existing screens only.